{
  "//": "TypeScript config — extends Astro's strict preset for typed component props.",
  "extends": "astro/tsconfigs/strict",
  "include": ["src/**/*", ".astro/types.d.ts"],
  "exclude": ["dist", "node_modules", ".vercel"]
}
